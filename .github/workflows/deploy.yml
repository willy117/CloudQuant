name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Node.js ⚙️
        uses: actions/setup-node@v4
        with:
          node-version: 20
          # 暫時移除 cache: 'npm' 以避免 "lock file not found" 錯誤

      - name: Install Dependencies 🔧
        # 使用 npm install 而非 npm ci，這樣即使沒有 package-lock.json 也能產生並安裝
        run: npm install

      - name: Build 🏗️
        run: npm run build
        env:
          # 在 Build 階段注入環境變數 (若有需要)
          VITE_FINNHUB_API_KEY: ${{ secrets.VITE_FINNHUB_API_KEY }} 
          VITE_FIREBASE_CONFIG_STRING: ${{ secrets.VITE_FIREBASE_CONFIG_STRING }}

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist
          branch: gh-pages
